# Generated by Ansible role openstack_kolla_minimal
[control]
{{ openstack_kolla_controller_host }} ansible_host={{ hostvars[openstack_kolla_controller_host].ansible_host }} ansible_user={{ openstack_kolla_ansible_user }} api_interface={{ openstack_kolla_tunnel_interface }} network_interface={{ openstack_kolla_tunnel_interface }}{% if openstack_kolla_private_key_file | length > 0 %} ansible_ssh_private_key_file={{ openstack_kolla_private_key_file }}{% endif %}

[network]
{{ openstack_kolla_controller_host }}

[loadbalancer]
{{ openstack_kolla_controller_host }}

[monitoring]
{{ openstack_kolla_controller_host }}

[compute]
{% for host in openstack_kolla_compute_hosts %}
{{ host }} ansible_host={{ hostvars[host].ansible_host }} ansible_user={{ openstack_kolla_ansible_user }} api_interface={{ openstack_kolla_tunnel_interface }} network_interface={{ openstack_kolla_tunnel_interface }}{% if openstack_kolla_private_key_file | length > 0 %} ansible_ssh_private_key_file={{ openstack_kolla_private_key_file }}{% endif %}
{% endfor %}

[storage]
{{ openstack_kolla_controller_host }}

[baremetal]
{% for host in openstack_kolla_hosts %}
{{ host }}
{% endfor %}

[deployment]
localhost ansible_connection=local

[baremetal:children]
control
network
compute
storage
monitoring

[common:children]
control
network
compute
storage
monitoring

[loadbalancer:children]
network

[mariadb:children]
control

[rabbitmq:children]
control

[keystone:children]
control

[glance:children]
control

[nova:children]
control

[neutron:children]
network

[openvswitch:children]
network
compute

[placement:children]
control

[heat:children]
control

[horizon:children]
control

[memcached:children]
control

[prometheus:children]
monitoring

[prometheus-node-exporter:children]
monitoring
control
compute
network
storage

[prometheus-cadvisor:children]
monitoring
control
compute
network
storage

[prometheus-alertmanager:children]
monitoring

[prometheus-openstack-exporter:children]
monitoring

[cron:children]
common

[fluentd:children]
common

[kolla-logs:children]
common

[kolla-toolbox:children]
common

[glance-api:children]
glance

[heat-api:children]
heat

[heat-api-cfn:children]
heat

[heat-engine:children]
heat

[keystone-fernet:children]
keystone

[keystone-ssh:children]
keystone

[nova-api:children]
nova

[nova-metadata:children]
nova

[nova-conductor:children]
nova

[nova-super-conductor:children]
nova

[nova-novncproxy:children]
nova

[nova-scheduler:children]
nova

[nova-spicehtml5proxy:children]
nova

[nova-serialproxy:children]
nova

[neutron-server:children]
control

[neutron-dhcp-agent:children]
neutron

[neutron-l3-agent:children]
neutron

[neutron-metadata-agent:children]
neutron

[neutron-ovn-metadata-agent:children]
compute
network

[neutron-bgp-dragent:children]
neutron

[neutron-infoblox-ipam-agent:children]
neutron

[neutron-metering-agent:children]
neutron

[ironic-neutron-agent:children]
neutron

[neutron-ovn-agent:children]
compute
network

[placement-api:children]
placement
